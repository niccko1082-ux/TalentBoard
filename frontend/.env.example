# Base URL of the TalentBoard REST API.
# Leave empty to use a same-origin "/api" path (Vite dev proxy or nginx reverse proxy).
# Set to e.g. http://localhost:8080 to call the backend directly (requires backend CORS).
VITE_API_BASE_URL=
